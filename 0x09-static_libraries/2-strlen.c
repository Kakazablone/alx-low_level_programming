#include "main.h"

/**
 * _strlen - main function
 * @s: character to get string
 * Return: the length of the string
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{}
	return (i);
}
